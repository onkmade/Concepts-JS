 1. Write a program to calculate the sum, difference, product, and quotient of two numbers `a = 15` and `b = 4`.

2. Use the modulo operator to determine if `num = 29` is even or odd, and log the result.

3. What is the final value of `y` after this code runs? `let y = 10; console.log(y++); console.log(++y);`

4. Convert 100 Celsius to Fahrenheit using the formula `F = (C × 9/5) + 32`.

5. Calculate the total cost of 3 items priced at `$12.50`, `$8.99`, and `$15.00` with a 7% sales tax.

6. Convert 250 minutes into hours and remaining minutes using division and modulo operators.

7. Find the average of four numbers: `45, 67, 82, 91`, and round to one decimal place.

8. Swap the values of `x = 7` and `y = 3` without using a third variable.

9. Extract the last digit from `number = 489` using the modulo operator.

10. Calculate simple interest for `principal = 5000`, `rate = 0.06`, and `time = 2` years using `Interest = P × R × T`.


```js
const num = 29;
console.log( num % 2 == 0);

let y = 10;
console.log(y++);
console.log(++y);

// convert the C to F, formula F = (C * 9/5) + 32;
let celcius = 26;
let F = (celcius * 9/5) + 32;
console.log(F);


const item1 = 12.50;
const item2 = 8.99;
const item3 = 15.00;
const total = item1 + item2 + item3;
const salestax = total * 7/100;
const result = total + salestax;
console.log(result);


const minutes = 250;
const hours = minutes/60;
const remaining_minutes = minutes % 60;
console.log(`${hours.toFixed(0)}:${remaining_minutes}`);

const num1 = 45;
const num2 = 67;
const num3 = 82;
const num4 = 91;
const totalNum = num1 + num2 + num3 + num4;
const numAvg = totalNum / 4;
console.log(numAvg); 

let sw1 = 7;
let sw2 = 3;
[ sw1, sw2 ] = [sw2, sw1];
console.log(sw1, sw2);


const exNum = 489;
const lastDigit = exNum % 10;
console.log(lastDigit);

const principal = 5000, rate = 0.06, time = 2;
const Interest = principal * rate * time;
console.log(Interest);
```